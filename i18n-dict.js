// English translations, keyed by the original Turkish source string/template -
// see i18n.js's header comment for why. Organized by source file, in the
// order those files load, purely for maintainability (nothing here depends on
// key order at runtime). A template key uses {name}-style placeholders,
// matching exactly the argument name passed to tf() at its call site.
const EN_DICT = {
};
